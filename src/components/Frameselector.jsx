/*
 * FrameSelector.jsx is no longer used in the main flow.
 *
 * Frame selection has been moved to FinalResult.jsx, after
 * the photos are taken. The FRAME_DEFS constant inside
 * FinalResult.jsx replaces the FRAMES constant here.
 *
 * You can safely delete this file, or keep it if you want
 * to repurpose the card UI elsewhere.
 *
 * The FRAMES export used elsewhere:
 *
 *   import { FRAMES } from './components/FrameSelector';
 *
 * → replace with the FRAME_DEFS object inside FinalResult,
 *   or export it from a shared constants file if needed.
 */